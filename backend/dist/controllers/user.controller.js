"use strict";
// Controlador de usuarios temporalmente deshabilitado
// TODO: Implementar funcionalidad de usuarios cuando sea necesario
/*
import { Request, Response } from 'express';
import prisma from '../config/database';
import { encrypt } from '../utils/encryption.utils';
import crypto from 'crypto';
import nodemailer from 'nodemailer';
import { env } from '../config/env';
import bcrypt from 'bcryptjs';

export const registerPatient = async (req: Request, res: Response) => {
  // Implementación pendiente
};

export const invitePatient = async (req: Request, res: Response) => {
  // Implementación pendiente
};

export const completePatientRegistration = async (req: Request, res: Response) => {
  // Implementación pendiente
};
*/ 
